# Backend Configuration

# Admin password for login (default: admin123)
ADMIN_PASSWORD=admin123

# Admin token for saving (default: local-admin-token)
ADMIN_TOKEN=local-admin-token

# Server port (default: 3000)
PORT=3000
